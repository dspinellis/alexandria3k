-- Wrapper to run Context Normalized Impact calculation via CLI options
-- Generic metric; resembles the Source Normalized Impact per Paper (SNIP) metric
.shell ./context_normalized_impact.py --test-db ././test_cni.db